section.intro
  .intro__wrapper
    h2.title.intro__title #{intro.title}
    p.intro__desc #{intro.desc}
    .intro__links
      each link in intro.links
        a(class=link.isAccent ? "button intro__link" : "intro__link" href=link.href) #{link.text}
